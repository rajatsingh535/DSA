void alphabets(char c1, char c2) {
    // Start from c1 and loop until c2 (inclusive)
    for (char i = c1; i <= c2; i++) {
        // Print the character followed by a space
        cout << i << " ";
    }
}